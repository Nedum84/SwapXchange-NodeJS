import request from "supertest";

describe("test to get user transactions", () => {
  it("should succeed when I add money and get my transactions", async () => {
    // const { tokens } = await global.signin();
    // const amount = 40000;
    // //Load money
    // await request(app)
    //   .post("/addmoney")
    //   .set("authorization", `bearer ${tokens?.access?.token}`)
    //   .send({
    //     amount,
    //   })
    //   .expect(200);
    // //get transactions
    // const res = await request(app)
    //   .get("/transaction")
    //   .set("authorization", `bearer ${tokens?.access?.token}`);
    // expect(res.body.data).toHaveProperty("wallet_transactions");
    // expect(res.body.data).toHaveProperty("transfers");
  });
});
